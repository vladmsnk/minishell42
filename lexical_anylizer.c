#include "includes/headers.h"
#include "includes/structs.h"

typedef struct s_token
{
	char *identifier;
	int		group_id;
	t_token *next;
} t_token;

t_token	*retrieve_tokens(char *str)
{
	int	i;
	t_token	*tokens;

	i = 0;
	while (splitted[i] != NULL)
	{

	}
}